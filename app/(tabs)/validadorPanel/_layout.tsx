import { Stack } from "expo-router";

export default function ValidadorLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
